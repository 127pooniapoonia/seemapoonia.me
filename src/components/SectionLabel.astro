---
interface Props {
  number: string;
}
const { number } = Astro.props;
---

<div class="section-label">
  <span class="text-brand font-bold tracking-widest text-xs uppercase font-sans">{number}</span>
</div>
